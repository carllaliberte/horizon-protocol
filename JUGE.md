# Juge

HORIZON du juge universel = date `YYYY-MM-DD` encore à venir.
`UFHY1` / `mldsa87` = suite du sceau, pas le champ horizon de `peut_dire`.

Schéma : https://github.com/carllaliberte/famille/blob/main/schema/juge.v0.json
